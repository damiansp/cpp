#include "Functions.h"  // optional but common


double add(double x, double y) {
  return x + y;
}

double add(double x, double y, double z) {
  return x + y + z;
}

bool test(bool x) {
  return x;
}

bool test(double x) {
  return x != 0;
}
